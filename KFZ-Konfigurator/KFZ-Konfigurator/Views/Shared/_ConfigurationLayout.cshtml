@using KFZ_Konfigurator.Helper;
@using KFZ_Konfigurator.Resources.Localization;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@RenderBody()

@if (ViewBag.IsModelSelection != true)
{
    @section CartOverview {
        <h1>Cart</h1>
    }
}

@section ConfigurationHeader {
    <div class="bg-light">
        <div class="container text-overlap-container">
            <img src="~/Resources/Images/@ViewBag.BackgroundImage" class="img-fluid" />
            <a href="@Url.RouteUrl(Constants.Routes.ModelOverview)" @Helper.If(ViewBag.IsModelSelection == true, "hidden")>
                <h2 class="overlap-top-left">Model</h2>
            </a>
        </div>
    </div>
    <nav class="navbar bg-dark navbar-dark navbar-expand-md">
        <div class="container @Helper.If(ViewBag.IsModelSelection == true, "invisible")">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-expanded="false" aria-label="Toggle" aria-controls="">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse" id="navbarToggleContent">
                <div class="navbar-nav">
                    <a href="@Url.RouteUrl(Constants.Routes.EngineSettings)" class="nav-item nav-link">@LocalizationManager.Localize("EngineSettingsHeader")</a>
                    <a href="@Url.RouteUrl(Constants.Routes.Accessories)" class="nav-item nav-link">@LocalizationManager.Localize("AccessoriesHeader")</a>
                    <a href="@Url.RouteUrl(Constants.Routes.Paints)" class="nav-item nav-link">@LocalizationManager.Localize("PaintsHeader")</a>
                </div>
            </div>
        </div>
    </nav>
}